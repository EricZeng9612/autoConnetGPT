# 安全说明

Auto Connect GPT 是本地守护程序，不是 VPN 服务或账号管理工具。它不读取聊天内容、订阅正文、ChatGPT 登录令牌或 GitHub 令牌；不开放公网端口。

启用节点恢复后，会通过本地 Mihomo 控制接口读取节点名称、类型、延迟和组选择，并修改**用户配置的一个 Selector 组**。节点名称、原选择和恢复事务保存在当前用户的私有运行目录，不应公开上传。

如果用户配置控制接口密钥，程序会读取当前用户拥有、权限为 600 的独立密钥文件，仅在请求头中使用；不会放到命令行、正常日志或通知里。控制接口只允许 loopback HTTP 或本地 Unix socket。请妥善保护控制接口本身；本工具不能防御已控制你本机账号的程序。

默认配置关闭节点切换；显式白名单优于名称识别。修改共享代理组可能影响其他应用流量，应选择最小影响范围。程序不会编辑订阅、路由规则、系统代理设置或清空现有连接。

Remote 和任务状态 JSON 是**可信输入**：可影响重启决策，必须由可验证的采集器生成，置于用户私有目录。程序不会执行其中的代码。不正确的 `idle` 证据可能导致运行中任务被中断；没有可靠采集器时请保持默认未知状态。

正常退出应用不是退出账号。程序不强杀 ChatGPT、不重置登录、不处理验证码。启动/重启计数、冷却、未完成回滚事务在动作前落盘，崩溃后保留；恢复状态损坏时安全停止，避免绕过限额。停止程序不能保证撤销已发出的网络请求，下一次检查会优先尝试恢复未完成事务。

诊断会向配置的公共 HTTPS 目标发送无账号凭据的探测请求；节点测速由 Mihomo 发起，也会产生网络流量。`--diagnose` 不执行应用恢复，但可能创建本地目录和锁文件。

日志按大小轮转，异常只记录类型以避免泄露响应内容。正常状态不包含节点名或密钥，但 `recovery.json` 会保存必要的节点事务。提交问题时请仅提供脱敏信息，不要上传整个安装目录。

报告漏洞：请优先使用仓库的私密漏洞报告功能（若可用）联系维护者；不要在公开 issue 中附上真实凭据、私有节点或可利用的敏感详情。
